name: developブランチの内容をもとに検証環境APIをデプロイ

on:
  push:
    branches:
      - develop
  workflow_dispatch:
jobs:
  ssh:
    runs-on: ubuntu-latest
    environment:
      name: "検証環境API"
      url: https://dev.api.seiryofes.com/docs
    steps:
      - name: 🔑Loading ssh private key...
        run: |
          mkdir -p .ssh && chmod 700 .ssh
          echo "$SSH_KEY" > .ssh/id_rsa && chmod 600 .ssh/id_rsa
        env:
          SSH_KEY: ${{ secrets.SSH_PRIVATEKEY }}
      - name: 🚀Deploy!!
        run: |
          ssh -t -t -o "StrictHostKeyChecking=no" -i .ssh/id_rsa $HOST "
            cd /home/ekke/hibiya-itchief/quaint-api-dev
            git checkout develop
            git pull
            docker-compose down
            docker-compose up -d --build
          "
        env:
          HOST: ${{ secrets.SSH_HOST }}
